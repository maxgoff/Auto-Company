# Local Wrangler state (miniflare D1/R2/KV simulation)
.wrangler/

# Local secrets for `wrangler dev` — NEVER commit.
# Real values live in `wrangler secret put`; see .dev.vars.example.
.dev.vars
.dev.vars.*
!.dev.vars.example

node_modules/
*.log
